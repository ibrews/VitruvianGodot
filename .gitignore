# Godot editor cache / imports (regenerated on open)
godot_project/.godot/
*.import

# OS / editor noise
.DS_Store
Thumbs.db
*.tmp
*.bak

# NOTE: unlike the MetaHumanGodot repo, the character assets here are CC0 and ARE
# committed (vitruvian_head.glb, vit_*.png). Do NOT add *.glb / *.png ignores.

# look-dev capture output
out/

# ── builds & renders (shipped as GitHub Release assets, not committed) ──
dist/
out/
# local Claude config / memory
.claude/
# backups + Hair Tool intermediate working files (the groom is rebuilt by build_vitruvian_hair.py)
*.bak
*.bak2
blender_prep/hairtool_out/
blender_prep/hairtool_work.blend
blender_prep/hairtool_work2.blend
blender_prep/vitruvian_hairtool.blend
blender_prep/facs_tmp/
